{% extends "layout/base.twig" %}

{% block title %}{{ code }} - xNocken{% endblock %}

{% block content %}
  <div class="container">
      <h1>{{ code }}</h1>
      <h2>{{ code|trans({
        ':path:': url
      }) }}</h2>
  </div>
{% endblock %}
